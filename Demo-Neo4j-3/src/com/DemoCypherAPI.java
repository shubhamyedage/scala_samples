/*
package com;

import org.neo4j.cypher.internal.ExecutionEngine;
import org.neo4j.cypher.internal.ExecutionResult;
import org.neo4j.graphdb.GraphDatabaseService;
import org.neo4j.graphdb.factory.GraphDatabaseFactory;

import java.io.File;

*/
/**
 * Created by synerzip on 2/6/16.
 *//*

public class DemoCypherAPI {

    static void main(String[] args){
        GraphDatabaseFactory dbFactory = new GraphDatabaseFactory();
        File src=new File("/home/synerzip/software/neo4j-community-3.0.1/data/databases/graph.db");
        GraphDatabaseService db= dbFactory.newEmbeddedDatabase(src);

        ExecutionEngine execEngine = new ExecutionEngine(db);
        ExecutionResult execResult = execEngine.execute("MATCH (n:Person) RETURN n")
        String results = execResult.dumpToString();
        System.out.println(results);
    }
}
*/
